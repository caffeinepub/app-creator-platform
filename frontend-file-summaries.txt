{
  "frontend/index.css": "Global dark theme CSS with OKLCH color tokens for Noventra.Ai's near-black background, indigo/cyan palette, Space Grotesk/Inter/JetBrains Mono fonts, glassmorphism utilities, glow effects, and animation keyframes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with red-orange OKLCH color tokens (brand, orange replacing indigo/cyan as primary palette), Space Grotesk/Inter/JetBrains Mono fonts, red-orange glow box shadows, and flame-flicker animation keyframes.",
  "frontend/src/App.tsx": "Root app with TanStack Router, QueryClientProvider, ThemeProvider (dark), Sonner Toaster, and ActorGuard wrapping the RouterProvider — ProfileSetup modal and all related imports/state have been completely removed so users go directly to the main interface.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks with `SessionExpiredError` class and `parseError` helper for 401 detection; removed the `useInitializeSystem` mutation that called the non-existent `actor.initialize()` method.",
  "frontend/src/services/llmService.ts": "LLM service that calls OpenRouter's Gemini model to generate complete HTML applications; includes `generateAIResponse` for scaffolding/modifying HTML and a standalone `extractHtmlFromResponse` utility with multiple fallback patterns (```html block, DOCTYPE, <html> tag).",
  "frontend/src/pages/HomePage.tsx": "Noventra.ai marketing landing page with hero, stats, features, and CTA sections; all `navigate` calls updated to use `/sessions/new` instead of the old `/new-session` route path.",
  "frontend/src/pages/SessionsPage.tsx": "Sessions dashboard with session cards; `navigate` calls updated to use `/sessions/new` and `/sessions/$sessionId` to match the current route tree.",
  "frontend/src/pages/NewSessionPage.tsx": "New session creation form; `navigate` call after successful creation updated to use `/sessions/$sessionId` and the back-link updated to `/sessions`.",
  "frontend/src/components/CodeBlock.tsx": "Mac-style code block with terminal chrome (traffic light dots), filename/language label, syntax-highlighted pre/code display in JetBrains Mono, and clipboard copy button.",
  "frontend/src/components/MessageBubble.tsx": "Chat message renderer with red-orange brand theme — user bubbles use red-orange gradient fill, assistant bubbles have subtle brand border, and the AI avatar uses brand-colored gradient background with Sparkles icon.",
  "frontend/src/pages/ChatPage.tsx": "Full chat interface with a `SessionErrorBanner` component that shows friendly \"Session expired\" messages with Retry and Logout buttons for 401 errors, replacing raw error text; also handles session-level load errors with a dedicated full-screen error state.",
  "frontend/src/index.css": "Global dark theme CSS with red-orange OKLCH color tokens (brand/orange palette replacing indigo/cyan), Space Grotesk/Inter/JetBrains Mono fonts, glassmorphism utilities, red-orange gradient-text and glow effects, btn-primary with red-orange gradient, and flame-flicker keyframe animation.",
  "frontend/src/components/LoginButton.tsx": "Internet Identity login/logout button updated with red-orange brand theme — primary state uses btn-primary (red-orange gradient), hover states reference brand color tokens, preserving all existing login/logout logic.",
  "frontend/src/components/ProfileSetup.tsx": "First-login profile setup modal with full error handling — wraps `saveProfile` in try/catch, displays inline error messages for unauthorized/not-ready/connection failures, clears errors on input change, and disables the input while saving.",
  "frontend/src/components/LivePreview.tsx": "Iframe live preview component that reactively creates a Blob URL from `htmlContent` via `useEffect`, revokes previous URLs to prevent memory leaks, uses `key={blobUrl}` on the iframe to force re-mount on updates, and shows an empty-state placeholder when no HTML is available.",
  "frontend/src/components/Logo.tsx": "New SVG flame logo component for Noventra.ai with a custom animated flame icon mark (outer/inner flame paths with red-orange gradients and glow filter) and a gradient wordmark; accepts size prop ('small' | 'large') for nav and hero contexts.",
  "frontend/src/components/ActorGuard.tsx": "Simplified initialization guard that waits for the actor to finish fetching (no `initialize()` call), proceeds to ready state immediately once `actorFetching` is false, with an 8-second hard timeout fallback and animated loading screen with progress dots."
}